<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppIot"
        android:usesCleartextTraffic="true">

        <!-- 1) Login como pantalla inicial (LAUNCHER) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppIot.Splash">   <!-- opcional: usa tu tema/splash aquí -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2) Home -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />

        <!-- 3) (Opcional) Deja MainActivity sin launcher o elimínala si no la usarás -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <!-- O elimina completamente este bloque si ya no se usa -->
    </application>
</manifest>